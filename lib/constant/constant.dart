import 'package:flutter/material.dart';

//i created constant file for using same decoration.
const kBoxDecoration = BoxDecoration(
  color: Colors.white,
  boxShadow: [BoxShadow(blurRadius: 5.0, offset: Offset(1.0, 5.0))],
);
